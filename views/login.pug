extends layouts/login-layout.pug

block content
    .loginContainer
        h1 Авторизація
        form(method="post")

            p.errorMessage #{errorMessage}

            //- input(type="text", name="logUsername", placeholder="Username or email", value=logUsername required="")
            //- input(type="password", name="logPassword", placeholder="Password", required="")

            input(type="text", name="logUsername", placeholder="Адреса електронної пошти", value="anna.stakhevich@nure.ua" required="")
            input(type="password", name="logPassword", placeholder="Пароль", value="anna8998" required="")

            input(type="submit", value="Увійти")
        a(href="/register") Потрібен акаунт? Зареєструйтеся тут.